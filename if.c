#include <stdio.h>
int main(int argc, char const *argv[])
{
    int marks = 82;
    /*if (expression)
        statements */
    if (marks > 33);
    //double %% is use for print % in printf
    printf("Congratulation,Your are passed with %d %%.", marks);

    return 0;
}
