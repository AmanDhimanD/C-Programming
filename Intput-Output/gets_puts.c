//gets get string value from kwyborad
#include <stdio.h>
int main()
{
    char str1[20], str2[20];
    puts("Enter name");
    gets(str1);
    puts ("Mixxe");
    gets(str2);//get valie of keyboard
    puts(str2);//print value of gets
    return 0;
}