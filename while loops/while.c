#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a = 900;

    printf("VIP number of JIO....\n");

    while (a<1000)
    {
        printf("\n9999999%d.",a);
        a++;
    }
    // both are same with For loops and While loops

    // for (int a = 1; a <= 10; a++)
    // {
    //     printf("\nNumber is %d.",a);
    // }
        return 0;
}
